<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.likelion.bebc25.itda.post.mapper.PostReactionMapper">

<!--    1. 좋아요 여부 확인-->
    <select id="countLike" resultType="int">
        SELECT COUNT(*)
        FROM post_reaction
        WHERE member_id = #{memberId} AND post_id = @{postId} AND type = 1
    </select>

<!--    2. 좋아요 등록-->
    <insert id="insertLike">
        INSERT INTO post_reaction(member_id, post_id, type)
        VALUES (#{memberId}, #{postId}, 1)
    </insert>

<!--    3. 좋아요 취소-->
    <delete id="deleteLike">
        DELETE FROM post_reaction
        WHERE member_id = #{memberId} AND post_id = #{postId} AND type = 1
    </delete>


</mapper>